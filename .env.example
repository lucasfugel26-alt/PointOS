# Supabase – optional in Phase 1.
# Wenn nicht gesetzt, läuft PointOS rein lokal (IndexedDB) und voll offline-fähig.
# Sobald gesetzt, synchronisiert die App im Hintergrund mit Supabase.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
